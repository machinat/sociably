import { makeNativeComponent } from '@sociably/core/renderer';
import { TELEGRAM } from '../constant';

const makeTelegramComponent = makeNativeComponent(TELEGRAM);

export default makeTelegramComponent;
