import { makeNativeComponent } from '@sociably/core/renderer';
import { TWITTER } from '../constant.js';

const makeTwitterComponent = makeNativeComponent(TWITTER);

export default makeTwitterComponent;
