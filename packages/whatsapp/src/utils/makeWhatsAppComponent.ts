import { makeNativeComponent } from '@sociably/core/renderer';
import { WHATSAPP } from '../constant';

const makeWhatsAppComponent = makeNativeComponent(WHATSAPP);

export default makeWhatsAppComponent;
