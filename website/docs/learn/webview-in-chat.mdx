---
title: Webview in Chat
---

> We have learned how to ship features in the chat.
> But sometimes chat UI are not enough for all the features.
> In this lesson, you'll learn how to open an webview to provide more features in GUI.
>
> _Time to accomplish: 15 minutes_

:::info
Example in this lesson use some Next.js and React.js features in the webview.
You can learn more about them here:

- [React.js](https://reactjs.org/docs/getting-started.html) - A JavaScript library for building user interfaces.
- [Next.js](https://nextjs.org/docs/getting-started) - The React Framework for Production.
:::

## Extend a Webview

Finally let's implement the deleting todo feature.
But this time, we are going to use a webview to display all the finisihed and unfinished todos.

### Open Webview

Follow the guide of the platform to add a button for opening the webview:


import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs
  groupId="chat-platforms"
  defaultValue="messenger"
  values={[
    {label: 'Messenger', value: 'messenger'},
    {label: 'Telegram', value: 'telegram'},
    {label: 'LINE', value: 'line'},
  ]}>
  <TabItem value="messenger">


Edit the `WithMenu` component like this:

```tsx {17-21} title="src/components/WithMenu.tsx"
    //...
    if (platform === 'messenger') {
      return (
        <>
          {children}
          <Messenger.ButtonTemplate
              buttons={
                <>
                  <Messenger.PostbackButton
                    title={listLabel}
                    payload={listData}
                  />
                  <Messenger.PostbackButton
                    title={addLabel}
                    payload={addData}
                  />
                  <Messenger.UrlButton
                    messengerExtensions
                    title="Edit 📤"
                    url={`https://${domain}/webview?platform=messenger`}
                  />
                </>
              }
          >
              {info}
          </Messenger.ButtonTemplate>
        </>
      );
    }
    //...
```

Here we add an `UrlButton` that links to `/webview` endpoint of our severer.
Two things to note here:
- Set the `messengerExtensions` prop to `true`.
- Add querystring param `platform=messenger`.


  </TabItem>
  <TabItem value="telegram">

Edit the `WithMenu` component like this:

```tsx {17-21} title="src/components/WithMenu.tsx"
    //...
    if (platform === 'telegram') {
      return (
        <>
          {children}
          <Telegram.Text
            replyMarkup={
              <Telegram.InlineKeyboard>
                <Telegram.CallbackButton
                  text={listLabel}
                  data={listData}
                />
                <Telegram.CallbackButton
                  text={addLabel}
                  data={addData}
                />
                <Telegram.UrlButton
                  login
                  text="Edit 📤"
                  url={`https://${domain}/auth/telegram`}
                />
              </Telegram.InlineKeyboard>
            }
          >
            {info}
          </Telegram.Text>
        </>
      );
    }
    //...
```

Here we add an `UrlButton` that links to `/auth/telegram` endpoint of our severer.
Note that the `login` prop have to be `true`.


  </TabItem>
  <TabItem value="line">

Edit the `WithMenu` component like this:

```tsx {20-23} title="src/components/WithMenu.tsx"
    //...
    if (platform === 'line') {
      return (
        <>
          {children}
          <Line.ButtonTemplate
            altText={`You have ${todoCount} todos now.`}
            actions={
              <>
                <Line.PostbackAction
                  label={listLabel}
                  displayText={listLabel}
                  data={listData}
                />
                <Line.PostbackAction
                  label={addLabel}
                  displayText={addLabel}
                  data={addData}
                />
                <Line.UriAction
                  label="Edit 📤"
                  uri={`https://liff.line.me/${lineLiffId}`}
                />
              </>
            }
          >
            {info}
          </Line.ButtonTemplate>
        </>
      );
    }
    //...
```

Here we add an `UriAction` that links to the [LIFF](https://developers.line.biz/en/docs/liff/overview/)
app we created at lesson 1.


  </TabItem>
</Tabs>


Now an `Edit 📤` button is added in the menu like this:

<img width={200} src={require("./assets/webview-button.png").default} />

Try tap the button and you should see the default webview is already working!

### Webview Client

The web front-end codes are in the `webview` directory.
Check `webview/pages/index.tsx` file and you'll see the `WebviewClient` constructed like this:


<Tabs
  groupId="chat-platforms"
  defaultValue="messenger"
  values={[
    {label: 'Messenger', value: 'messenger'},
    {label: 'Telegram', value: 'telegram'},
    {label: 'LINE', value: 'line'},
  ]}>
  <TabItem value="messenger">

```tsx title="webview/pages/index.tsx"
// ...
const client = new WebviewClient({
  mockupMode: typeof window === 'undefined',
  authPlatforms: [
    new MessengerWebviewAuth({
      appId: publicRuntimeConfig.messengerAppId,
    }),
  ],
});
// ...
```

  </TabItem>
  <TabItem value="telegram">

```tsx title="webview/pages/index.tsx"
// ...
const client = new WebviewClient({
  mockupMode: typeof window === 'undefined',
  authPlatforms: [
    new TelegramWebviewAuth({
      botName: publicRuntimeConfig.telegramBotName,
    }),
  ],
});
// ...
```

  </TabItem>
  <TabItem value="line">

```tsx title="webview/pages/index.tsx"
// ...
const client = new WebviewClient({
  mockupMode: typeof window === 'undefined',
  authPlatforms: [
    new LineWebviewAuth({
      liffId: publicRuntimeConfig.lineLiffId,
    }),
  ],
});
// ...
```

  </TabItem>
</Tabs>


After the `client` is constructed, it logs in user and open a connection to the server.
We can then use it to communicate with the server.

### Webview Page

Let's display all the todos in the webview. Edit the index page to this:

```tsx {1,5-14,19,27-39} title="webview/pages/index.tsx"
import { TodoState } from '../../src/types';
// ...

const WebAppHome = () => {
  const data = useEventReducer<null | TodoState>(
    client,
    (currentData, { event }) => {
      if (event.type === 'app_data') {
        return event.payload.data;
      }
      return currentData;
    },
    null
  );

  return (
    <div>
      <Head>
        <title>Edit Todos</title>
        <link
          rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/water.css@2/out/water.css"
        />
      </Head>

      <main>
        <h4>You have {data ? data.todos.length : '?'} Todo:</h4>
        <table>
          <tbody>
            {data?.todos.map((todo) => <tr><td>{todo.name}</td></tr>)}
          </tbody>
        </table>

        <h4>You have {data ? data.finishedTodos.length : '?'} finished Todo:</h4>
        <table>
          <tbody>
            {data?.finishedTodos.map((todo) => <tr><td>{todo.name}</td></tr>)}
          </tbody>
        </table>
      </main>
    </div>
  );
};
// ...
```

:::info
The JSX in the webview is [React.js](https://reactjs.org) element.
While the _Machinat_ JSX is rendered into chat messages,
the _React_ JSX is rendered into HTML content.
:::

The `useEventReducer` [hook](https://reactjs.org/docs/hooks-reference.htm)
is the simplest way to handle events from server. Every time a event is received,
the reducer function is called to update the data.

```ts
const data = useEventReducer(client, reducerFn, initialValue);
```

Because there is no data now, the webview should look like this:

<img width={500} src={require("./assets/webview-1.png").default} />


### Communicate to Webview

On the server side, we have to send the todos data to the webview.
Edit the `handleWebview` handlers to this:

```tsx title="src/handlers/handleWebview.tsx"
import { makeContainer } from '@machinat/core/service';
import TodoController from '../services/TodoController';
import { WebAppEventContext } from '../types';

const handleWebview = makeContainer({
  deps: [TodoController],
})(
  (todoController) =>
    async (ctx: WebAppEventContext & { platform: 'webview' }) => {
      const { event, bot, metadata: { auth } } = ctx;

      if (event.type === 'connect') {
        const { data } = await todoController.getTodos(auth.channel);

        return bot.send(event.channel, {
          category: 'webview_push',
          type: 'app_data',
          payload: { data },
        });
      }
    }
);

export default handleWebview;
```

The `bot.send(channel, eventObj)` method sends an event to the webview.
Here we emit an `'app_data'` event every time a webview `'connect'`.

The `metadata.auth` object contains the authorization infos.
The `auth.channel` refers to the original chatroom,
so we can use `TodoController` to get todos data.

Now the webview should display the todos like this:

<img width={500} src={require("./assets/webview-2.png").default} />


### Send Event to Server

Let's add a button to delete a todo. Edit the index page like this:

```tsx {2-19,34,39,46} title="webview/pages/index.tsx"
// ...
const TodoRow = ({ todo }) => (
    <tr>
      <td style={{ verticalAlign: 'middle' }}>{todo.name}</td>
      <td style={{ textAlign: 'right' }}>
        <button
          onClick={() => {
            client.send({
              category: 'webview_action',
              type: 'delete_todo',
              payload: { id: todo.id },
            })
          }}
        >
          ❌
        </button>
      </td>
    </tr>
);

const WebAppHome = () => {
  // ...
  return (
    <div>
      <Head>
        <title>Edit Todos</title>
        <link
          rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/water.css@2/out/water.css"
        />
      </Head>

      <main>
        <h3>Press ❌ to delete todos</h3>

        <h4>You have {data ? data.todos.length : '?'} Todo:</h4>
        <table>
          <tbody>
            {data?.todos.map((todo) => <TodoRow todo={todo} />)}
          </tbody>
        </table>

        <h4>You have {data ? data.finishedTodos.length : '?'} finished Todo:</h4>
        <table>
          <tbody>
            {data?.finishedTodos.map((todo) => <TodoRow todo={todo} />)}
          </tbody>
        </table>
      </main>
    </div>
  );
};
//...
```

We add a `❌` button on every `TodoRow` to delete the todo.
Now the webview should look like this :

<img width={500} src={require("./assets/webview-3.png").default} />

The `client.send(eventObj)` method sends an event back to ther server.
Here we emit a `'delete_todo'` event when the `❌` button is tapped.

We can then handle it at server side like this:

```tsx title="src/handlers/handleWebview.tsx"
  // ...
  if (event.type === 'delete_todo') {
    const { todo, data } = await todoController.deleteTodo(
      auth.channel,
      event.payload.id
    );

    return bot.send(event.channel, {
      category: 'webview_push',
      type: 'app_data',
      payload: { data },
    });
  }
  //...
```

We delete the todo in the state when a `'delete_todo'` event is received.
Then emit an `'app_data'` event to refresh the data.

Now the todos can be deleted in the webview like this:

<video width={450} autoplay="autoplay" loop inline muted>
  <source src={require("./assets/webview-delete.webm").default} type="video/webm" />
</video>

---

Congratulation! 🎉 You have finished the Machinat app tutorial. Now you are able to
combine **JSX Chat UI**, **Services**, **Dialog Scripts** and **Webview** to build
a feature-rich app with amazing experiences in chat.

Here are some resources you can go next:

- Learn more about Machinat at our [Documents](/docs).
- Check the complete [Todo Example](https://github.com/machinat/todo-example/).
  You can find some omitted features there, like [paging](https://github.com/machinat/todo-example/blob/main/src/components/ShowTodos.tsx)
  and [editing todo](https://github.com/machinat/todo-example/blob/main/webview/pages/index.tsx#L46-L65).
- Check more examples:
  - [Note Example](https://github.com/machinat/note-example) - take notes in the webview.
  - [Pomodoro Eample](https://github.com/machinat/pomodoro-example) - pomodoro timer in chat.
  - [4digits Example](https://github.com/machinat/4digits-example) - play guessing 4 digits game in chat.
- Visit [GitHub discussions](https://github.com/machinat/machinat/discussions) to ask a question.
- Follow our [Twitter](https://twitter.com/machinatjs) to know any updates.
